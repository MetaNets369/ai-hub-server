version: '3.8'
services:
  mcp-jina-ai:
    build: https://github.com/joebuildsstuff/mcp-jina-ai.git
    container_name: mcp-jina-ai
    ports:
      - "8081:8080"
    environment:
      - JINA_API_KEY=${JINA_API_KEY}
    restart: unless-stopped
