version: '3.8'
services:
  mcp-jina-ai:
    build: https://github.com/jina-ai/MCP.git
    container_name: mcp-jina-ai
    ports:
      - "8081:8080"
    environment:
      - JINA_API_KEY=${JINA_API_KEY}
    restart: unless-stopped
